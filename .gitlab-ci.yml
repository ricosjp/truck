stages:
    - test
    - pages

cpu-test:
    image: rust:latest
    stage: test
    cache:
        - /root/.cargo/bin/cargo-make
    script:
        - cargo install cargo-make
        - cargo make ci-cpu-test

gpu-test:
    image: registry.ritc.jp/ricos/truck/truck/rust-vulkan:_no_branch
    stage: test
    script:
        - /root/.cargo/bin/cargo make ci-gpu-test
    tags:
        - gpu
    artifacts:
        paths:
            - images

pages:
    image: rust:latest
    stage: pages
    script:
        - cargo doc --no-deps
        - mv target/doc public
    artifacts:
        paths:
            - public
    only:
        - master
